CREATE TABLE players(
                        id           SERIAL PRIMARY KEY,
                        name         VARCHAR(100),
                        market_value VARCHAR,
                        age          INT
)